# Base de datos MongoDB
DB_MONGO=mongodb://localhost:27017/ecuamazon

# Secret para JWT
TOKEN_SECRET=tu_secret_jwt_muy_seguro_aqui

# Credenciales AWS S3 para upload de imágenes
AWS_ID=tu_aws_access_key_id
AWS_SECRET=tu_aws_secret_access_key
AWS_BUCKET_NAME=tu-bucket-s3-name

# Puerto del servidor (opcional, por defecto 4000)
PORT=4000